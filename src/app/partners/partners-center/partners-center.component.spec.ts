import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartnersCenterComponent } from './partners-center.component';

describe('PartnersCenterComponent', () => {
 /*let component: PartnersCenterComponent;
  let fixture: ComponentFixture<PartnersCenterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartnersCenterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartnersCenterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
