import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartnersSidenavComponent } from './partners-sidenav.component';

describe('PartnersSidenavComponent', () => {
  /*let component: PartnersSidenavComponent;
  let fixture: ComponentFixture<PartnersSidenavComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartnersSidenavComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartnersSidenavComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
