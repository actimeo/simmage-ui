/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ObjectivesListResolve } from './objectives-list-resolve.guard';

/*describe('ObjectivesListResolveService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ObjectivesListResolveService]
    });
  });

  it('should ...', inject([ObjectivesListResolveService], (service: ObjectivesListResolveService) => {
    expect(service).toBeTruthy();
  }));
});*/
