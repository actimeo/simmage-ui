/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ResourcesListResolve } from './resources-list-resolve.guard';

/*describe('ResourcesListResolveService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ResourcesListResolveService]
    });
  });

  it('should ...', inject([ResourcesListResolveService], (service: ResourcesListResolveService) => {
    expect(service).toBeTruthy();
  }));
});*/
