import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TeamsCenterComponent } from './teams-center.component';

describe('TeamsCenterComponent', () => {
  /*let component: TeamsCenterComponent;
  let fixture: ComponentFixture<TeamsCenterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TeamsCenterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TeamsCenterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
