import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TeamsSidenavComponent } from './teams-sidenav.component';

describe('TeamsSidenavComponent', () => {
  /*let component: TeamsSidenavComponent;
  let fixture: ComponentFixture<TeamsSidenavComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TeamsSidenavComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TeamsSidenavComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
