import { TestBed, inject } from '@angular/core/testing';

import { DialogEventDetailsService } from './dialog-event-details.service';

describe('DialogEventDetailsService', () => {
 /* beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DialogEventDetailsService]
    });
  });

  it('should ...', inject([DialogEventDetailsService], (service: DialogEventDetailsService) => {
    expect(service).toBeTruthy();
  }));*/
});
