import { TestBed, inject } from '@angular/core/testing';

import { FormLeaveDialogService } from './form-leave-dialog.service';

describe('FormLeaveDialogService', () => {
  /*beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormLeaveDialogService]
    });
  });

  it('should ...', inject([FormLeaveDialogService], (service: FormLeaveDialogService) => {
    expect(service).toBeTruthy();
  }));*/
});
