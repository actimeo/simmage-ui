import { TestBed, inject } from '@angular/core/testing';

import { FormsDialogService } from './forms-dialog.service';

describe('FormsDialogService', () => {
  /*beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormsDialogService]
    });
  });

  it('should ...', inject([FormsDialogService], (service: FormsDialogService) => {
    expect(service).toBeTruthy();
  }));*/
});
