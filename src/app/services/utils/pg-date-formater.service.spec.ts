import { TestBed, inject } from '@angular/core/testing';

import { PgDateFormaterService } from './pg-date-formater.service';

describe('PgDateFormaterService', () => {
  /*beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PgDateFormaterService]
    });
  });

  it('should ...', inject([PgDateFormaterService], (service: PgDateFormaterService) => {
    expect(service).toBeTruthy();
  }));*/
});
