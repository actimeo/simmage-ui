import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EventsSummaryComponent } from './events-summary.component';

describe('EventsSummaryComponent', () => {
  /*let component: EventsSummaryComponent;
  let fixture: ComponentFixture<EventsSummaryComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EventsSummaryComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EventsSummaryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
