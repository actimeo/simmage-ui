import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DossierSituationFilterComponent } from './dossier-situation-filter.component';

describe('DossierSituationFilterComponent', () => {
/*  let component: DossierSituationFilterComponent;
  let fixture: ComponentFixture<DossierSituationFilterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DossierSituationFilterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DossierSituationFilterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
